# KT Planner configuration.
# Copy this file to ".env" and fill in real values. The app loads .env
# automatically on startup via python-dotenv.

# ----------------------------------------------------------------------------
# Gemini (required for coverage text polishing / professionalization)
# ----------------------------------------------------------------------------
# Get a key from https://aistudio.google.com/app/apikey
# Without this key, coverage text falls back to a basic local cleanup only.
GEMINI_API_KEY=AQ.Ab8RN6KUqqbdkYhHrBoPIdylSpbod0RCxEqFTNWa2QWmEzzGIQ

# Optional: override the default model.
# GEMINI_MODEL=gemini-2.5-flash
